en:
  mail_form:
    errors:
      models:
        contact:
          attributes:
            email:
              invalid: "is not a valid email address"
              
  activerecord:
    errors:
      models:
        user:
          attributes:
            location:
              not_geocoded: "not found"
            nickname:
              invalid: "allows only numbers, letters, as well as '.' and '-'"
            phoneno:
              invalid: "allows only numbers, as well as '-' and ' '"
            current_password:
              blank: "is needed"
              invalid: "does not match"
            email:
            terms:
              accepted: "must be accepted"
            password_confirmation:
              confirmation: "does not match"
            confirmation_token:
              invalid: "is invalid"

        article:
          attributes:
            value_eur:
              greater_than_or_equal_to: "may not be negative"
            deposit_eur:
              greater_than_or_equal_to: "may not be negative"
            rate_eur:
              greater_than_or_equal_to: "may not be negative"
            rate_intervall:
              inclusion: " must be one of [hour, day, week, month, year]"


  errors:
    messages:
      taken: "is already taken"
      blank: "may not be empty"
      not_a_number: "is not a number"
      too_short: "is too short"
      too_long: "is too long"
