<% articles ||= Article.all %>

// prepare the marker data
var marker_defs, bounds;

// build up bounds vector
bounds = <%= "#{@bounds.to_s} "%>

// add house markers
marker_defs = [ ];

// add current location marker
marker_def_center = <%= create_map_current_location_marker(@current_location) %>;

// draw the map
drawmap_leaflet("mapid", marker_defs, marker_def_center, bounds);
